<h1>Top Rated Movies</h1>

<% @movies.each do |movie| %>
    <div id="movie-<%=movie.id%>">
        <ul><%= link_to movie.title, "/movies/#{movie.id}" %> -- Rating: <%= movie.rating %>/10</ul>
    </div>
<% end %>